---
apiVersion: apps/v1
kind: Deployment
metadata: 
  name: iperf3-client
spec:
  replicas: 1
  selector:
    matchLabels:
      application: iperf3-client
  template:
    metadata:
      labels:
        application: iperf3-client
    spec:
      containers: 
        - image: quay.io/skupper/simple-iperf
          name: iperf3-client
          command:
            - iperf3
            - "-s"
          imagePullPolicy: IfNotPresent